# Environment (dev/staging/prod)
ENVIRONMENT=local

# Database (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=collabite
DB_USER=collabite_admin
DB_PASSWORD=changeme

# Cognito (fill after terraform apply)
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
COGNITO_REGION=us-east-1

# S3
MEDIA_BUCKET=collabite-media-dev

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Stripe (fill with your test keys)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Meta / Social verification
META_APP_SECRET=
META_WEBHOOK_VERIFY_TOKEN=
META_PAGE_ACCESS_TOKEN=
INSTAGRAM_OTA_HANDLE=collabite_mx
TIKTOK_OTA_HANDLE=collabite_mx
MANUAL_VERIFICATION_ADMIN_TOKEN=
